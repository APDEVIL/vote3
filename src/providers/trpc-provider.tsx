"use client";

/**
 * providers/trpc-provider.tsx
 *
 * Re-export of the T3 TRPCReactProvider — kept here so index.tsx
 * imports from one place only.
 */

export { TRPCReactProvider as TRPCProvider } from "@/trpc/react";